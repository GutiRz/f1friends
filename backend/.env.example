# Copia este archivo a .env y ajusta los valores para tu entorno local.
# El archivo .env nunca debe subirse al repositorio (está en .gitignore).

PORT=8080

DB_HOST=localhost
DB_PORT=5432
DB_USER=f1friends
DB_PASSWORD=f1friends
DB_NAME=f1friends
DB_SSLMODE=disable

JWT_SECRET=cambia-esto-por-un-secreto-seguro

# Origen permitido para CORS (frontend Next.js en desarrollo)
CORS_ORIGIN=http://localhost:3000
